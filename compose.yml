services:
    bot:
        image: docker.io/node:16.9.1-alpine
        container_name: rdgg_bot
        restart: unless-stopped
        volumes:
            - ./:/app
        working_dir: /app
        command: sh -c 'npm i && npm run start'
